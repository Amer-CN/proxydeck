# 更新日志

## v3.0.0 · ProxyDeck

### 🚀 全新界面

- **换上机械风操作面板**：整个界面按 PommeToys 风格重新设计——奶油色机身、顶部红绿灯、黑色点阵屏、点火拉杆，双击即用
- **更名为 ProxyDeck**：工具已从单一 CommandCode 代理成长为多平台托管代理，正式更名并换新图标
- **点火拉杆**：启动代理不再是点按钮，而是把红色拉杆从左拉到右，伴随自检灯仪式和机械音效；运行中拉回最左即熄火
- **上屏信息牌**：顶部黑色显示屏实时显示运行状态、运行时长里程表、各代理绿灯，滚动页面时还有音量条随动
- **滚动有声音了**：滚动页面发出真实滚轮的棘轮咔哒声，滚得越快越密，滚到头自动静音
- **设置页**：开机自启、界面音效（三挡）、界面语言（七种）、版本信息、检查更新统一收纳
- **彻底关机拨杆**：平时关窗口后台代理继续跑（老规矩）；设置页拨下拨杆才会连后台一起关

### ✨ 体验优化

- **悬停看模型价格**：鼠标停在任意模型上 1 秒，顶部屏幕显示该模型的价格和上下文参数
- **支持七种语言**：简体、繁体、英语、德语、日语、法语、西班牙语一键切换，整个界面跟着变
- **帮助和模型参考**：三个代理页面都能打开使用帮助和全模型价格速查表
- **消耗统计离线可见**：代理没运行也能看到历史消耗，不再是一片空白

### 🐛 Bug 修复

- **修复消耗统计不显示**：官网统计接口没就绪时本地兜底数据被错误拦截，导致面板一直空白
- **修复模型悬停无反应**：GO 套餐的模型名和价格表的命名格式不一致，悬停查询永远落空
- **修复拉杆点火后自己弹回**：点火进行中的一处状态刷新会误把拉杆弹回起点，约五次点火出现一次
- **修复滚动声音偶发延迟**：浏览器挂起音频引擎后第一声会被丢弃，现在唤醒后立即补上

## v2.5.4 · K3 节奏器 · 日志防抖

- **KIMI-K3 限流节奏器**：开启后 8788 自动排队放行 + 429 时代为等待重发（总预算 30 分钟），客户端只看到慢请求不再报错，可通宵挂长任务
- 修复：团结视图日志面板每 3 秒重复刷「积分消耗 TOP 已装载」
- 429 透传：不再换 key 盲重试，携带上游 Retry-After

## v2.5.3 · 流式响应误判修复

- 修复：流式请求空响应嗅探不再用 3 秒窗口判定——GLM-5.3 复杂请求首个内容延迟超 3 秒时曾被误判为空流强制重放，客户端表现为连接重置/超时
- 仅在流读到 EOF 仍无内容时才重放；慢启动的正常流直接放行

## v2.5.2 · 团结签名适配 · chat 可靠性

- **适配团结 LiteLLM 安全校验**：上游强制校验签名请求头，缺失返回 401；自动生成签名（算法逆向自官方客户端）
- **chat 可靠性**：会话 id 改为每次请求随机（固定会话会粘死在不稳定实例上）；思考型响应不再被误判为空；重试间隔放宽

## v2.5.1 · 稳定性 · 体验优化

- **插件端口接管复用**：GUI 启动插件时若端口已有健康实例直接接管，正在使用的客户端不断线
- **统一后台管理**：打开 GUI 即识别全部插件端口运行状态，一键启停，后台进程独立于 GUI 存续
- 团结付费模型上架标注（KIMI-K3 高价警示；GLM-5.3 费率）

## v2.5.0 · 四大插件视图

- **新增团结（Codely）插件**：自动读取登录态，积分/配额实时监测，模型矩阵按厂商分组
- **新增 WorkBuddy（CodeBuddy）插件**：桌面端登录态直连，密钥自动兑换，内置零宽脱敏
- **新增 Notion AI 插件**：CDP 自动读取桌面端令牌，配额查询，多工作空间切换
- **新增灵犀（WPS）插件**：自动读取登录态，灵点余额/月额度实时监测

## v2.4.0 · Go 原生架构升级 · 插件托管

- **Go 原生重写**：代理核心从 Python 迁移为 Go 单文件（启动快、内存省）
- **插件托管架构**：每个插件为独立后台进程，关 GUI 不中断服务
- 开发者模式（隐藏入口）与插件健康检查

## v2.3.2 · 思考内容透传

- **推理过程透传**：上游通过 reasoning-delta 事件返回思考内容，此前被静默丢弃；现完整透传给客户端

## v2.3.1 · 延迟测试多行显示

- 延迟测试结果分多行显示（每个目标一行）；模型矩阵字号还原

## v2.2.0 · 稳定版 · 图标 · 自愈

- **应用图标**：反应堆风格图标（16-256px）嵌入 exe——资源管理器/任务栏/标题栏均显示
- **崩溃自愈**：后台代理异常退出后自动重新点火（用户主动停止除外）
- 更新检查：启动后自动查 GitHub 最新版，有新版顶部横幅提示

## v2.1.0 · 官网权威统计

- **官网账单直连**：金额/总 Token/运行次数/套餐额度直接来自官方接口，每 8 秒自动刷新
- 性能优化与内存占用下降

## v2.0.0 · 从 HTA 升级为独立程序

- **WebView2 独立 GUI**：弃用 HTA 控制台，重做为单 exe 科幻全息控制台（无需安装）
- 代理常驻后台，关窗不停止

## v1.1.2 · 本地使用统计

- 新增总消耗 Token / 今日消耗 Token 本地统计（替代无法查询的官方额度）

## v1.1.1 · 已使用额度补全

- 「已使用额度」界面补全：总 Token 拆分、运行次数、明细表，对齐官方 Usage 网页

## v1.1.0 · 已使用额度查询

- 新增「已使用额度」查询：套餐名称、已用金额、剩余额度、使用比例、剩余天数

## v1.0.5 · 修复空消息 400

- 修复续长对话时 400 报错（Codex 历史中的推理摘要等无文本条目转换后变成空内容消息）
- 精简为单文件版

## v1.0.4 · 模型配置参考

- 新增「模型配置参考」面板：上下文窗口/最大输出/价格速查，数据来自官方文档

## v1.0.3 · 界面升级

- 渐变头部 + 版本徽章，分区化布局，按钮质感提升

## v1.0.2 · 修复续对话静默

- 修复 Codex 历史会话续对话时思考后静默停止（超 1MB 的单行事件被读取器丢弃导致响应截断）

## v1.0.1 · 修复 max_tokens 超限

- 修复 400 错误：部分 Agent 按模型上下文窗口发送超大 max_tokens（如 100 万）超过接口上限 20 万

## v1.0.0 · Windows 便携工具

- 首个版本：CommandCode 订阅转本地 OpenAI 兼容接口 + HTML 控制台 + 一键启停

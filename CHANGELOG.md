## v3.6.4 · 注水检测传真机 FX-01

### 🚀 新功能

- **注水检测独立传真机页（FX-01）**：设置/注水甲板一键进入整页弹窗，原版 pomme-ui 传真机逐字复刻（580×784 结构冻结、全动线时间轴对齐官方、窄窗滚动不压缩），标题栏红圆点=返回
- **真检测接线**：纸面渠道/模型下拉与甲板双向镜像，发送即全流程检测（前 3.2 秒官方时间轴照播、等待态不显假百分比），回执回填真实判定/相似度/账号/用时，失败可重试，撕纸刷新甲板历史
- 新增 `docs/fidelity-audit.md` 忠实度判定说明书

## v3.6.3 · B.AI 模型矩阵 · 境外泄漏收口

### 🚀 新功能

- **B.AI 甲板模型矩阵上线**：8891 新增 GUI 专用 `/model/matrix`（复用注水渠道共享配置里的 B.AI key，带 Authorization 打上游 `/v1/models`，上游 `data` 原样透传），甲板按厂商分组显示全部真实模型；裸名（无 provider 前缀）分组走兜底拆分——取分隔符前导段去尾数，`hy3`→HY3、`qwen3.8-27b`→QWEN，新厂商上架不必改表
- **拉杆刷新，官方增删自动跟随**：拉杆点火与「↻ 刷新」各强制实时拉一次，3 秒轮询只吃进程内缓存——官网零轮询铁律延伸到 B.AI（实测 18 分钟内上游 44→42，`mimo-v2.5` / `mimo-v2.5-pro` 下架，代码零改动即跟上）
- 缓存语义：失败负缓存 60 秒（期内不重试）、刷新失败保留旧数据不清空、并发冷启动单飞（8 个并发只打上游 1 次）；无 key 显示 `NO KEY` +「未配置 B.AI 密钥（注水检测页填入）」，失败显示 `ERR` +「上游拉取失败 · 60 秒内不重试」，不再退化成 401 空白；副行改显「缓存于 HH:MM」

### 🐛 Bug 修复

- **B.AI 甲板三处境外泄漏收口**：`/v1/models` 无 key 纯透传必 401（模型矩阵因此从来没出过数据）、`loadPluginMeta` 每 3 秒把 `/model/info`、`loadPluginStats` 每 3 秒把 `/v1/stats` 打到 api.b.ai（上游没有这两个端点，Cloudflare 一律 403）——三处全部改为不再出网，`/v1/models` 语义保持逐字节透传不变
- **`fetchOnRun` 闩锁漏清**：原先由 tj/cb 分支各自清，B.AI 无积分分支会漏清，一旦拉杆刷新就会退化成 3 秒强刷打官网；改为 `refreshPluginView` 顶部一次性消费
- 删除从未被任何 JS 绘制过的 `baiFree` 死灯（「免费」角标是静态 CSS 徽章，保留）；`baStModelsSub` 占位文案「实时拉取」改「拉杆刷新」

## v3.6.2 · K3 429 兜底 · 账号池接管

### 🚀 新功能

- **K3 节奏器改造为 429 兜底**：上游 KIMI-K3 限流口径 2026-08 已改（429 体不再含 used/cap，改为 `Sliding window TPM limit exceeded for model_group=KIMI-K3 provider_ref=… window=60s`，多带 `Retry-After: 60` 头；原 cap=200000 滑窗记账前提失效），改造为纯响应式 429 兜底：删本地 cap/主动排队，改按上游 Retry-After 等待后原样重发（缺省 60s，恰为一个窗口），单请求总预算 30 分钟，预算内客户端永不看到 429
- **账号池路径接入兜底**：原 429 兜底只挂在单账号路径，账号池下 K3 流量实际不受保护；本次把兜底循环搬到账号池路径（与 402 换号、瞬态重试并列），双路径统一语义
- **GUI 同步**："K3 节奏"开关文案/i18n 七语改为"K3 429 兜底"，去掉已无意义的窗口用量展示；"等待中 N" 表示当前被兜底接管的进行中请求数

## v3.6.1 · 判定变更告警

### 🚀 新功能

- **疑似官方判定变更告警**：上游 401 命中「请升级 / x-codely-signature」（官方明示签名被判无效）即时告警；换 cli_api_key 后仍连续 401≥3 次亦告警——团结视图顶部红色警示条提示「检查官方 CLI 更新」，附命中信号与发生时间，可关闭，恢复 200 自动解除；/health 暴露 judgment_alert / judgment_signal / judgment_since 供脚本监测

### 🐛 Bug 修复

- appVersion 停在 v3.4.0（v3.5.0 / v3.6.0 两次升版漏同步）——补齐至 v3.6.1

## v3.6.0 · 模型多选分配 · 官网零轮询 · 鼓轮重排

### 🚀 新功能

- **账号矩阵模型多选**：账号卡点「✎ 编辑」弹出全部模型勾选列表，勾选即分配、保存即生效；多账号认领同一模型自动轮询分摊，付费号认领付费模型、免费号兜底（替代手动输入模型名）
- **积分面板账号选择器**：团结多账号可切换查询各自积分（后端按指定账号 token 拉取）

### ✨ 体验优化

- **官网请求零轮询定稿**：COMMAND 官网用量改事件驱动（拉杆启动/手动刷新各拉一次，删 15s/8s 定时器与无限重试循环）；各插件积分同规——拉杆启动自动拉一次，之后全靠手动刷新，杜绝官网 429 限流
- **未启动也能看**：媒体转路由/注水检测未点火时显示缓存内容 + 「需启动」提示，不再整页空白；外部账号区改紧凑卡 + 折叠表单
- **Mode 鼓轮重排**：B.AI 紧挨 COMMAND（同一颗键的两张脸，切换只滚一格）；子页顺序改为 设置 → 媒体转路由 → 注水检测，与传送带一致
- 子页面字号放大（检测报告可细读）；VU 音量条只在四个主页显示；启动横幅/更新提醒改浮窗

### 🐛 Bug 修复

- 模型矩阵混入 Agnes 等外部模型（`?full=true` 兜底路径误合并 provider 条目）——矩阵回归纯团结原生模型；聊天客户端 `/v1/models` 仍可见外部模型
- 赠送池显示漏算每日赠送积分（显示 1 万实际 1 万多）——赠送池+日赠剩余合并显示
- 团结积分「刷新」按钮失效（绑定被统一循环静默覆盖）——恢复强制实时刷新
- 链路诊断「本地代理健康」行移除（读数无诊断价值）
- 文档脱敏：tuanjie2api README 移除调试期遗留的真实 key（该 key 按 24h TTL 设计早已失效，仍按惯例轮换处理）

## v3.5.0 · 账号池回归 · B.AI 回归 · 反封号联审

### 🚀 新功能

- **账号池回归**：自动探测改一次性会话（每次探测=全新未登录窗口，可连续添加多账号）；本地客户端账号入列显示（本地+池内同屏，来源徽章区分）；探测/手动添加 token 预验证（无效凭据不再静默入池）
- **B.AI 回归**：COMMAND 键单击切换副页（COMMAND⇄B.AI），副页态保持按压+灯变蓝+键名切换；8891 端口 Go 栈透明转发，协议怪癖适配，上下文超限自动压缩
- **GLM-5.3-FLASH 加入原生识图白名单**（不再被媒体改路由劫持）
- **反封号联审实施**：metadata 四字段注入（prompt_id/session_id/cwd/litellm_conversation_id，对齐官方 buildMetadata）+ 会话生命周期随机化（闲置 20~40min、存活 3~5.5h，去机器特征）+ key TTL 1h→24h（401 被动失效+账号指纹检测双兜底）

### 🐛 Bug 修复

- 媒体转路由弹窗报 tjvisModel is not defined——补回 4 个丢失的 var 声明（严格模式 IIFE 赋值未声明变量即崩）
- 自动探测每 2 秒强刷登录页（导航兜底被长轮询反复触发）——已移除，探测窗口可安静登录

## v3.4.0 · 注水检测多渠道化 · 积分口径修正

### 新增
- 注水检测覆盖全部四个渠道：团结（账号池）/ Command / WorkBuddy / B.ai——
  渠道选择器 + 各渠道模型列表联动，一键检测任意渠道模型真伪
- 探针目标抽象：非团结渠道探针直打本地端点；基准按 渠道|模型 分库，
  旧基准自动迁移
- B.ai 渠道密钥配置：tuanjie-water-channels.json（不入库）

### 修复
- 团结积分显示与官方套餐不符（根本原因）：官网 coding_plan.windows
  三窗口（5h 限额 / 周限额 / 月限额）此前完全漏解析，daily_allowance
  被误当套餐总量。现按套餐口径显示：月套餐剩余 / 周窗口 / 5h 窗口 /
  今日消耗，与官网 LITE 套餐（16000/4000/800）一致
- 思考型模型（deepseek/GLM/KIMI）探针与分布采样 max_tokens 自适应
  （8→96），修复推理段烧预算导致检测值缺失
- ZCode 400「欢迎使用Codely」根治（实测 2026-08-26）：上游反竞品检测——
  system 消息出现 "You are <竞品名>" 身份声明（ZCode/Claude Code/Codex
  实测被拦；user 消息/工具描述不查；Cursor/无关名放行）即拒。反代转发前
  对 system 的该模式插入零宽空格（You‌are）打断匹配，模型语义无损

### 其他
- 探针 URL 双重 /v1 拼接修复
- 团结「由于安全问题请升级到最新版」401 排查定案：官方 rc.55 签名机制与
  rc.54 逐函数同构（种子 406f…、消息 v1\npath\nts、两层 HMAC 派生均不变，
  源码实证），该 401 为无效签名的标准响应（非换锁）；cli UA 版本号对齐官方
  rc.55；账号 team 现被限制为 GLM-5.3 / KIMI-K3 / codely-* 别名模型（上游
  /v1/models 动态反映，请求白名单外模型返回 team_model_access_denied）
- 反代特征收敛（社区逆向情报，对齐官方 CLI 行为画像）：x-litellm-session-id
  改租借制会话池——空闲复用（串行请求恒同一会话=单窗口连续对话）、全忙新建
  （=再开一个 CLI 窗口）、闲置 30 分钟清理（=关窗口）、4 小时轮换（=CLI
  重启）、池上限 16 超限共享；UA 启动时
  动态读本机 npm 全局 codely-cli 版本（CLI 升级代理自动跟随，读不到回退
  rc.55），新增会话池行为单测 6 项

# 更新日志

## v3.3.0 · B.AI 回归 · 双击翻页

### 🚀 新功能

- **B.AI 回归**：双击当前选中键翻副页（COMMAND⇄B.AI），三键格局不变、容量翻倍；副页态保持按压 + 状态灯变蓝 + 键名切换，状态一目了然
- **B.AI 插件（8891）**：Go 栈透明转发 api.b.ai，协议怪癖适配（developer→system、effort max→high、max_tokens 钳 8192、5xx 自动重试、上游代理自探测）

### 🗑️ 移除

- **Notion / 灵犀彻底移除**：internal 包、插件注册、子模式、flag 全部清除

## v3.2.0 · 请求体对齐 · uTLS 指纹

### 🚀 新功能

- **请求体对齐官方 CLI**：转发前按官方 `buildCreateParams` 字段序重组（model 在前、stream/stream_options 在尾），补齐官方默认字段 `parallel_tool_calls:true` / `metadata.user_id`（稳定 device-id）/ `litellm_session_id` / `prompt_cache_key`（与 x-litellm-session-id 头同值）；非流式请求删除 stream/stream_options 字段（官方行为）——请求形态与官方 CLI 一致
- **uTLS 指纹伪装**：TLS ClientHello 换 Chrome/BoringSSL 系指纹（HelloChrome_Auto，与官方 CLI 的 Node.js 同源），Clash 代理场景走自建 CONNECT 隧道内嵌指纹握手，消除 Go 原生 TLS 的反代识别特征；socks5 代理回落标准行为

## v3.1.0 · 单账号模式 · 请求头对齐

### 🚀 新功能

- **团结视图改单账号模式**：彻底移除账号矩阵/账号池/CDP 自动探测，登录态只读 `~/.codely-cli/oauth_creds.json`（读桌面端登录态），杜绝多账号轮换的封号风险；注水检测探针同步改走本地登录态

### ✨ 体验优化

- **团结请求头对齐官方 CLI**：User-Agent 换为官方真值 `Codely-CLI - OSS/1.0.0-rc.54 (Codely-Cli/1.0.0-rc.54)`，补齐 `X-DashScope-CacheControl` / `X-DashScope-UserAgent` 标记头，降低反代识别特征
- **CommandCode 模型目录同步官方 61 个**（Go 套餐 36 个），旧前缀名保留为兼容别名

### 🗑️ 移除

- 删除账号池相关代码：`accounts.go`（AccountPool）、`cdp_browser.go`（CDP 浏览器探测）、`features_test.go` 中账号池测试
- 删除 UI 账号矩阵：矩阵按钮/灯/卡片/自动探测/手动添加全部移除，团结 tab 显示单账号登录态

## v3.0.0 · ProxyDeck

### 🚀 全新界面

- **换上机械风操作面板**：整个界面按 PommeToys 风格重新设计——奶油色机身、顶部红绿灯、黑色点阵屏、点火拉杆，双击即用
- **更名为 ProxyDeck**：工具已从单一 CommandCode 代理成长为多平台托管代理，正式更名并换新图标
- **点火拉杆**：启动代理不再是点按钮，而是把红色拉杆从左拉到右，伴随自检灯仪式和机械音效；运行中拉回最左即熄火
- **上屏信息牌**：顶部黑色显示屏实时显示运行状态、运行时长里程表、各代理绿灯，滚动页面时还有音量条随动
- **滚动有声音了**：滚动页面发出真实滚轮的棘轮咔哒声，滚得越快越密，滚到头自动静音
- **设置页**：开机自启、界面音效（三挡）、界面语言（七种）、版本信息、检查更新统一收纳
- **彻底关机拨杆**：平时关窗口后台代理继续跑（老规矩）；设置页拨下拨杆才会连后台一起关

### ✨ 体验优化

- **悬停看模型价格**：鼠标停在任意模型上 1 秒，顶部屏幕显示该模型的价格和上下文参数
- **支持七种语言**：简体、繁体、英语、德语、日语、法语、西班牙语一键切换，整个界面跟着变
- **帮助和模型参考**：三个代理页面都能打开使用帮助和全模型价格速查表
- **消耗统计离线可见**：代理没运行也能看到历史消耗，不再是一片空白

### 🐛 Bug 修复

- **修复消耗统计不显示**：官网统计接口没就绪时本地兜底数据被错误拦截，导致面板一直空白
- **修复模型悬停无反应**：GO 套餐的模型名和价格表的命名格式不一致，悬停查询永远落空
- **修复拉杆点火后自己弹回**：点火进行中的一处状态刷新会误把拉杆弹回起点，约五次点火出现一次
- **修复滚动声音偶发延迟**：浏览器挂起音频引擎后第一声会被丢弃，现在唤醒后立即补上

## v2.5.4 · K3 节奏器 · 日志防抖

### 🚀 新功能

- **KIMI-K3 限流节奏器**：开启后 8788 自动排队放行 + 429 时代为等待重发（总预算 30 分钟），客户端只看到慢请求不再报错，可通宵挂长任务

### 🐛 Bug 修复

- 修复：团结视图日志面板每 3 秒重复刷「积分消耗 TOP 已装载」
- 429 透传：不再换 key 盲重试，携带上游 Retry-After

## v2.5.3 · 流式响应误判修复

### 🐛 Bug 修复

- 修复：流式请求空响应嗅探不再用 3 秒窗口判定——GLM-5.3 复杂请求首个内容延迟超 3 秒时曾被误判为空流强制重放，客户端表现为连接重置/超时；仅在流读到 EOF 仍无内容时才重放，慢启动的正常流直接放行

## v2.5.2 · 团结签名适配 · chat 可靠性

### 🚀 新功能

- **适配团结 LiteLLM 安全校验**：上游强制校验签名请求头，缺失返回 401；自动生成签名（算法逆向自官方客户端）

### ✨ 体验优化

- **chat 可靠性**：会话 id 改为每次请求随机（固定会话会粘死在不稳定实例上）；思考型响应不再被误判为空；重试间隔放宽

## v2.5.1 · 稳定性 · 体验优化

### ✨ 体验优化

- **插件端口接管复用**：GUI 启动插件时若端口已有健康实例直接接管，正在使用的客户端不断线
- **统一后台管理**：打开 GUI 即识别全部插件端口运行状态，一键启停，后台进程独立于 GUI 存续
- 团结付费模型上架标注（KIMI-K3 高价警示；GLM-5.3 费率）

### 🐛 Bug 修复

- 修复：Notion/灵犀视图 PLUGIN 标识错标为 CODEBUDDY

## v2.5.0 · 四大插件视图

### 🚀 新功能

- **团结（Codely）插件**：自动读取登录态，积分/配额实时监测，模型矩阵按厂商分组
- **WorkBuddy（CodeBuddy）插件**：桌面端登录态直连，密钥自动兑换，内置零宽脱敏
- **Notion AI 插件**：CDP 自动读取桌面端令牌，配额查询，多工作空间切换
- **灵犀（WPS）插件**：自动读取登录态，灵点余额/月额度实时监测

## v2.4.0 · Go 原生架构升级 · 插件托管

### 🚀 新功能

- **Go 原生重写**：代理核心从 Python 迁移为 Go 单文件（启动快、内存省）
- **插件托管架构**：每个插件为独立后台进程，关 GUI 不中断服务
- 开发者模式（隐藏入口）与插件健康检查

## v2.3.2 · 思考内容透传

### 🚀 新功能

- **推理过程透传**：上游通过 reasoning-delta 事件返回思考内容，此前被静默丢弃；现完整透传给客户端

## v2.3.1 · 延迟测试多行显示

### ✨ 体验优化

- 延迟测试结果分多行显示（每个目标一行）；模型矩阵字号还原（用户反馈改大后不协调）

## v2.2.0 · 稳定版 · 图标 · 自愈

### 🚀 新功能

- **应用图标**：反应堆风格图标（16-256px）嵌入 exe——资源管理器/任务栏/标题栏均显示
- **崩溃自愈**：后台代理异常退出后自动重新点火（用户主动停止除外）
- **更新检查**：启动后自动查 GitHub 最新版，有新版顶部横幅提示

## v2.1.0 · 官网权威统计

### 🚀 新功能

- **官网账单直连**：金额/总 Token/运行次数/套餐额度直接来自官方接口，每 8 秒自动刷新
- 性能优化与内存占用下降

## v2.0.0 · 从 HTA 升级为独立程序

### 🚀 新功能

- **WebView2 独立 GUI**：弃用 HTA 控制台，重做为单 exe 科幻全息控制台（无需安装）
- 代理常驻后台，关窗不停止

## v1.1.2 · 本地使用统计

### 🚀 新功能

- 本地使用统计：新增总消耗 Token / 今日消耗 Token 本地统计（替代无法查询的官方额度）

## v1.1.1 · 已使用额度补全

### 🚀 新功能

- 「已使用额度」界面补全：总 Token 拆分、运行次数、明细表，对齐官方 Usage 网页

## v1.1.0 · 已使用额度查询

### 🚀 新功能

- 新增「已使用额度」查询：套餐名称、已用金额、剩余额度、使用比例、剩余天数

## v1.0.5 · 修复空消息 400

### 🐛 Bug 修复

- 修复续长对话时 400 报错（Codex 历史中的推理摘要等无文本条目转换后变成空内容消息）
- 精简为单文件版
- 修复：「刷新状态」按钮报函数未定义

## v1.0.4 · 模型配置参考

### 🚀 新功能

- 新增「模型配置参考」面板：上下文窗口/最大输出/价格速查，数据来自官方文档

## v1.0.3 · 界面升级

### ✨ 体验优化

- 界面升级：渐变头部 + 版本徽章，分区化布局，按钮质感提升

## v1.0.2 · 修复续对话静默

### 🐛 Bug 修复

- 修复 Codex 历史会话续对话时思考后静默停止（超 1MB 的单行事件被读取器丢弃导致响应截断）

## v1.0.1 · 修复 max_tokens 超限

### 🐛 Bug 修复

- 修复 400 错误：部分 Agent 按模型上下文窗口发送超大 max_tokens（如 100 万）超过接口上限 20 万

## v1.0.0 · Windows 便携工具

### 🚀 新功能

- 首个版本：CommandCode 订阅转本地 OpenAI 兼容接口 + HTML 控制台 + 一键启停
- 复审修正（主会话 code-reviewer，2026-08-27）：B3 追踪头移除——rc.55 源码实证
  telemetry 默认 enabled:!1 且 NodeSDK 未注册 B3 propagator（官方默认不发该 5 头，
  发了反而是识别信号）；A/B 实测无 B3 头大小请求均 200，401 修复与 B3 无关
- 诊断勘误（官方客户端复核指出，2026-08-27）：此前"TLS WAF 拦截/401 修复靠 UA"
  均为误诊——真实原因是直连测试脚本漏了 Authorization Bearer 头（补上后
  Python 直连 rc.54/rc.55 UA 均 200）。UA 跟随保留（无害且自动适配版本），
  "直连实测"仍是最有效的排查路径

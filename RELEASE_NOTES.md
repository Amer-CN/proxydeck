# CommandCode 代理控制台 / Windows Launcher Tools

给 [command-code-proxy-server](https://github.com/dev2k6/command-code-proxy-server)（原作者 **dev2k6**，本仓库核心代码来自上游）
做的 Windows 便携工具：本地代理 + 科幻全息控制台 + 一键启停 + 开机自启。

## v2.5.4（2026-08-22）—— K3 节奏器 · 日志防抖

- **移除实验性的 ZCode 多开功能**（该路线已被官方服务端判定拦截，详见 README/提交历史）
- **新增 KIMI-K3 限流节奏器（GUI 可开关）**：团结上游对 KIMI-K3 有个人滑动窗口限流（TPM 200K/60s），长请求打满后 ZCode 端 429 重试风暴、任务断线。开启后 8788 自动排队放行 + 429 时代为等待重发（总预算 30 分钟），客户端只看到"慢请求"不再报错——可通宵挂长任务。团结视图顶栏「K3 节奏」按钮一键启停，重启记忆；官方取消限流后可随时关闭
- **修复**：团结视图日志面板每 3 秒刷"积分消耗 TOP 已装载"（轮询无防重）；模型矩阵日志改为仅首次装载
- 429 透传：不再换 key 盲重试，携带上游 Retry-After

## v2.5.3（2026-08-21）—— 流式响应误判修复

- **修复**：流式请求空响应嗅探不再用 3 秒窗口判定（GLM-5.3 复杂请求首个内容 chunk 延迟超过 3 秒时被误判为空流并强制重放，客户端表现为 ECONNRESET/超时）
- 现在仅在流读到 EOF 仍无任何内容时才重放；慢启动的正常流直接放行透传

## v2.5.2（2026-08-21）—— 团结签名适配 · chat 可靠性

- **适配团结 LiteLLM 安全校验**：上游（约 2026-08-21 起）强制校验 `X-Codely-Signature` 请求头，缺失返回 401「由于安全问题，请升级到最新版 Codely」。自动生成签名（算法逆向自官方客户端 codely-cli 1.0.0-release.52），User-Agent 同步升级
- **chat 可靠性修正**：会话 id 改为每次请求随机（上游按会话亲和路由，固定会话会粘死在不稳定实例上）；空响应嗅探补识别 `reasoning_content`（思考型响应不再被误判为空）；错误重试/重放间隔放宽，避免短窗连发刺激上游
- **同步修补** `plugins/tuanjie2api/codely2api.py`（同一签名算法，防旧脚本踩同一坑）

## v2.5.1（2026-08-19）—— 稳定性 · 体验优化

- **插件端口接管复用**：GUI 启动插件时若端口已有健康实例（CLI/其他工具拉起），直接接管复用、不杀不重启——正在使用该服务的客户端（子智能体等）不断线；仅清理非健康占用
- **统一后台管理**：打开 GUI 即识别全部插件端口的运行状态，一键启停，后台进程独立于 GUI 存续
- **模型信息增强**：团结付费模型 GLM-5.3 / KIMI-K3 上架标注（KIMI-K3 含高价警示 16积分/1M输入；GLM-5.3 标注费率）
- **修正**：Notion/灵犀视图 PLUGIN 标识错标为 CODEBUDDY 的显示问题

## v2.5.0（2026-08-18）—— 四大插件视图 · 界面提示

- **新增团结（Codely）插件视图**：自动读取 `~/.codely-cli` 登录态，积分/配额实时监测，模型矩阵按厂商分组
- **新增 WorkBuddy（CodeBuddy）插件视图**：桌面端登录态直连腾讯后端，`cli_api_key` 自动兑换（1h 缓存），内置零宽脱敏避免审核误拦
- **新增 Notion AI 插件视图**：CDP 自动读取桌面端 `token_v2`，配额/积分实时查询，支持多工作空间切换
- **新增灵犀（WPS）插件视图**：自动读取桌面端登录态，灵点余额/月额度实时监测
- **设置面板**：统一收纳版本（UI/核心）/ 累计下载 / 关于 / 检查更新；累计下载恢复历史展示口径；检查更新失败给出明确提示（不再无限"检查中"）
- **界面提示**：Notion / 灵犀为对话协议，不支持工具调用（不可作为 Agent/子智能体），顶栏已标注"仅限对话 · 无工具调用"
- 全部插件以独立后台进程运行，关闭主窗口不中断

## v2.4.0（2026-08-14）—— Go 原生架构升级

- **单二进制分发**：代理核心与界面完全 Go 原生（CGO WebView2），不再依赖 Python 运行环境
- **模型悬停浮窗**：新增「AGENT 填写」速查区（上下文 / 最大输出 ≤200000 / 思考级别说明）
- **模型参考升级**：55 个模型完整速查表（按上下文分组、点击模型名复制）
- **消耗统计增强**：链路监测 6 卡 + 各模型消耗 TOP（对数条形图、缓存读/写明细）
- **稳定性**：延迟测试走代理链路测 TTFB、僵尸端口自清理、异常自愈重启
- 开机自启、空格键点火、关窗提示等既有功能全部保留

## v2.3.2（2026-08-13）—— 思考内容透传（DeepSeek 方言 reasoning_content）（2026-08-13）—— 思考内容透传（DeepSeek 方言 reasoning_content）

- **推理过程透传**：上游 CommandCode 一直通过 `reasoning-delta` 事件返回思考内容，
  但此前代理未解析该事件类型，思考过程被静默丢弃（只保留最终答案）。现完整透传：
  - 流式响应：`delta.reasoning_content` 增量推送（DSH 等客户端实时渲染思考过程）
  - 非流式响应：`message.reasoning_content` 字段 + `usage.completion_tokens_details.reasoning_tokens` 统计
- `reasoning_effort` 档位（off/low/medium/high/max）行为差异现在可见：off 档思考极少、
  max 档完整推理链
- 内核 v1.2.0：新增 reasoning 事件解析与透传

## v2.3.1（2026-08-13）—— 延迟测试多行显示 · 界面微调（2026-08-13）—— 延迟测试多行显示 · 界面微调

- **延迟测试改版**：结果分多行显示（每个目标一行），字体调大；代理输入框/按钮收窄，不再超出面板边界
- **模型矩阵字号还原**：模型芯片字体从 12px 调回 11px（用户反馈改大后不如原来协调）
- **DSH（DeepSeek Harness）兼容修复**：DSH 以 `role:"developer"` 发送系统提示，代理此前未识别该角色导致
  带工具调用的多轮对话被上游 400 拒绝（`messages[0].role` 校验失败）；现与 `system` 同等提取，
  未知角色兜底为 `user`，`reasoningEfforts` 各档位（off/low/medium/high/max）实测全部可用
- 新增 `-debug` 启动参数：打印请求/响应体到 headless-error.log，便于排查协议问题
- **延迟测试稳健性**：代理探测超时 1.2s→4s、目标超时 2.5s→6s（梯子链路抖动大）；探测目标改用国内站（百度）确认代理可用，避免 CommandCode 链路慢时误判"代理不可用"；7897 排在候选端口首位
- **模型推理 TTFB**：测延迟时若代理运行，额外发一个 1-token 最小请求测首 token 时间（模型"卡不卡"的真实指标，非网络 RTT）
- 代理地址占位符不再写死示例端口（避免误导填错）

## v2.3.0（2026-08-12）—— 模型目录全量 · 套餐切换 · 费用浮窗 · 性能与可读性

- **模型目录全量 55 个**：`/v1/models` 从写死的 18 个升级为官方完整目录（与 Available Models 一致，按厂商分组）
  - 新增 Claude 全系（Opus/Sonnet/Haiku/Fable）、GPT-5.3~5.6 全系、Kimi K3/K2.7、GLM-5.2、Qwen 3.8、
    Muse Spark、Grok 4.5、Laguna S 2.1（免费）、Ling 3.0 Flash（免费）、Fugu、Inkling、Nemotron 等
- **套餐切换（与官方一致）**：模型矩阵新增「全部 55 / Go 32」切换按钮
  - `?plan=go` 返回 Go 套餐内的 32 个模型（官方同款）；默认视图为 **Go 32**（当前套餐实际可用）
- **费用浮窗**：悬停模型名自动弹出官网单价（输入/输出/缓存读/缓存写/上下文窗口），折扣角标（-50%~-99%）与「FREE」标记
- **短名映射补全**：`MapModel` 覆盖全部 55 个模型的短名→完整 ID（大小写不敏感、忽略分隔符；`opus`→Opus 5、`grok`→Grok 4.5 等）
- **性能优化**：星空/星云动画在窗口最小化/隐藏时暂停；面板视差与浮窗定位改为 rAF 节流（消除鼠标扫过的卡顿）；
  星空 canvas 增加 DPI 适配（高分屏更锐利，DPR=1 行为不变）
- **可读性**：模型矩阵、模型消耗 TOP、运行日志、标签等文字整体放大；模型消耗 TOP 名称不再截断（换行显示 + 悬停看全名）
- **交互修正**：移除模型矩阵的系统悬停 title（不再遮挡费用浮窗）
- 新增模型目录 / 套餐过滤 / 短名映射的单元测试（全量 55、Go 32、全模型自映射）

## v2.2.0（2026-08-07）—— 稳定版 · 图标 · 高分屏 · 自愈 · 更新检查

- **应用图标**：反应堆风格图标（16-256px 多尺寸）嵌入 exe——资源管理器 / 任务栏 / 标题栏均显示
  （修复：webview 底层窗口类使用系统默认图标，现通过 WM_SETICON 显式覆盖）
- **后台常驻稳定**：headless 日志落盘（`headless-error.log`，异常可查）；GUI 检测到代理异常退出时**自动重新点火**（最多 3 次，防死循环）
- **高分屏适配**：窗口初始尺寸按屏幕工作区自适应（2K/4K 自动放大，不超出屏幕；DPI-aware 逻辑像素）
- **版本更新检查**：启动时异步查 GitHub 最新 Release，发现新版显示横幅一键直达下载页（离线静默）
- **120Hz / 高刷新率**：EQ 条与扫描光束改为合成器动画（scaleY/translateY），高刷屏无重排抖动；全部持续动画均走 RAF/合成器
- **界面**：底部双栏「运行日志 | 模型消耗 TOP」；统计卡片 2 列等高布局；文字对比度提亮；`--dim/--dim2` 升级；移除校准功能（官网直连数据）
- 中文乱码修复（手动刷新日志文案）
- **长对话上下文修复**：全程历史接近 1M 上限时（如 84 万 token 的会话），
  `max_tokens` 自动压缩到剩余空间；上游若仍以 context 超限拒绝，代理解析报错中的
  精确 token 数并自动重发一次——不再出现"模型未返回任何内容"（实测 240 万字符请求 200 OK）

## v2.1.0（2026-08-07）—— 官网权威统计 · 性能优化

- **官网账单直连**：金额 / 总 Token / 运行次数 / 套餐额度直接来自 CommandCode 官方接口
  （`/alpha/usage/summary` 等，个人账号无组织自动直调），卡片标注「官网金额」，每 8 秒自动刷新
- **彻底解决金额对不上**：缓存命中按官网账单口径计费（缓存读取 $0.0028/1M），本地 token 拆分不再参与金额计算
- **统计卡片 2 列布局**：9 位以上数字完整显示，不再溢出
- **API Key 保存按钮**：输入即自动保存（失焦或点「保存」），后台进程重启自动读取；显示/隐藏改为输入框内 👁 图标
- **性能修复**：官网接口结果缓存 20 秒——之前 GUI 每 8 秒同步阻塞 2-3 秒拉官网，webview 消息循环被冻结导致界面卡顿；现在轮询毫秒级返回
- **统计链路修复**：`user_` 前缀 Key 曾被 `http.CanonicalHeaderKey` 破坏大小写导致官网接口 401（已修复）；`/v1/usage` 4 个上游接口并行化（3.3s → ~2s）
- 本地统计（分模型 / 今日）保留为官网不可用时的兜底，模型条可点击校准
- 关闭窗口后代理保持后台运行（headless 常驻），统计不受影响

## v2.0.0（2026-08-06）—— WebView2 独立程序 · 界面全部重做

- **全新控制台**：HTA 退役，改为正式的 Windows 独立程序 `ProxyDeck.exe`（WebView2 渲染）
  - 科幻全息界面：3D 反应堆核心（点击点火/停堆，空格键亦可）、星空/透视网格/星云/扫描线背景、
    玻璃拟态面板 + 鼠标 3D 视差、Toast 操作反馈、开机自检动画
  - 统计卡片数字滚动动画 + 分模型消耗条形图；模型矩阵按厂商分组、点击复制
  - 深色沉浸式标题栏、高 DPI 自适应、关窗自动优雅停堆
- **架构重做**：代理核心直接在主程序进程内运行（不再释放/调用 `bin\` 下的 exe）——点火秒开，无解压等待
- **体积不变大**：WebView2 运行时随系统/Edge 预装，无需打包进 exe
- 新增 `-headless` 无窗口后台模式（开机自启由此驱动）
- 新增外部实例检测：开机自启的后台代理已在跑时，控制台自动接入监测，可一键停止
- 新增 `构建EXE.bat` 一键构建脚本（自动拉依赖 + 编译出单个 exe）
- 旧版 `bin\stats.json` 统计数据自动迁移到 exe 同目录
- **移除**：`CommandCode代理-单文件版.hta`、`hta_template.txt`、`生成单文件版.py`、`bin/` 预编译产物
  （均仍存在于 git 历史与旧 Release 中）
- ⚠️ **迁移提示**：旧版开机自启脚本指向已删除的 `bin\command-code-proxy.exe`，
  升级后请在新控制台里重新开关一次「开机自启」（或运行一次新的 设置开机自启.bat）

## v1.0.1（2026-08-06）

- **修复**：`max_tokens` 超限导致的 400 错误——代理层统一钳制到 200000（API 上限），并新增单元测试
- 单文件版 / 多文件版同步更新（内嵌修复后的代理）

## v1.0.0（2026-08-06）

- 首个发布：单文件版 HTA、HTML 控制台、启停/开机自启脚本、单文件构建工具

## 使用

1. 运行 `构建EXE.bat` 生成 `ProxyDeck.exe`，双击打开 → 点核心点火
2. 在任意 OpenAI 兼容客户端 / Agent 里配置：
   - Base URL: `http://127.0.0.1:55990/v1`
   - API Key: 你自己在 CommandCode Studio 生成的 Key
3. 可用 32 个 Go 套餐模型（全部 55 个可切换查看）：DeepSeek V4、Kimi K2.x/K3、GLM-5.x、Qwen 3.x、MiniMax、小米 MiMo、GPT-5.6 Luna、Grok 4.5 等

> 代理会把 OpenAI 格式请求转发到 CommandCode 的 `/alpha/generate`（CLI 内部接口），
> 因此**不需要 Provider 套餐**，普通套餐即可使用。

## 注意

- 仅支持 Windows；界面依赖系统自带的 WebView2 运行时（一般已预装）
- 未签名程序，SmartScreen / 杀软首次提示请选「仍要运行 / 允许」
- 需要你自己的 CommandCode 账号与套餐

## 致谢

代理核心来自 [dev2k6/command-code-proxy-server](https://github.com/dev2k6/command-code-proxy-server)。
